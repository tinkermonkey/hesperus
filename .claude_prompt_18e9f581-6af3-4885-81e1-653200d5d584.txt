You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 4: Interactive state matrix in demo

**Description**:
For every interactive Flowbite component section in `App.jsx`, add a systematic state matrix showing default, hover, active/pressed, focus-visible, disabled, and (where applicable) loading states simultaneously. Currently components are shown in default state only.

## Requirements
- FR-9.1: Default state shown for every interactive component
- FR-9.2: Hover state shown or annotated (bg↔fg flip where applicable)
- FR-9.3: Active/pressed state shown (1px translate for buttons)
- FR-9.4: Focus-visible state shown with the 2px orange focus ring visually present
- FR-9.5: Disabled state shown (opacity 0.4, `cursor-not-allowed`)
- FR-9.6: Loading state shown for Button, Spinner, and Progress
- Applies to: Button, TextInput, Select, Checkbox, Radio, ToggleSwitch, Dropdown, Accordion, Tabs, Pagination, and all other interactive Flowbite components in the demo

## Design Guidance
Implements the Interactive State Coverage phase from the architecture design. Visual targets are `foundations-states.html` and `components-buttons-states.html` in `design/design_handoff_hesperus/system/preview/`. The FLOWBITE_MAPPING.md state contract for each component defines the exact token and behavior expected at each state.

## Acceptance Criteria
- [ ] Every interactive component section shows at minimum: default, focus-visible, disabled states as static renders
- [ ] Hover and active/pressed states are shown (either as persistent CSS class overrides or with an interactive trigger)
- [ ] Focus-visible state uses the Hesperus 2px orange focus ring, not a browser default outline
- [ ] Disabled state shows opacity 0.4 and `cursor-not-allowed` cursor
- [ ] Button, Spinner, and Progress sections include a loading state example
- [ ] State matrix layout is consistent across all component sections

## Dependencies
Phase 1, Phase 3

## Parent Issue
Part of #1


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git